s p am
Yaedt
dUTF-8 encoded samam
yaedt
dUTF-8 encod encoded sample plain-text file
‾‾‾‾‾‾‾�$�‾‾‾‾‾tences:

  ∮ E⋅da = Q,  n → ∞, ∑ f(i) = ⊏ g(i), ∀x∈ℝ: ⌈x⌉ = −⌊−x⌋, α ∧ ¬β¬(¬α ∨ β),

  ℕ ⊆ ℕ₀ ⊂ �ed sample plain-text file
‾‾‾‾‾‾‾�$�‾‾‾‾‾tences:

  ∮ E⋅da = Q,  n → ∞, ∑ f(i) = ⊏ g(i), ∀x∈ℝ: ⌈x⌉ = −⌊−x⌋, α ∧ ¬β¬(¬α ∨ β),

  ℕ ⊆ ℕ₀ ⊂ ℤ ⊂ ℚ ⊂ ℝ ⊂ ℂ, ⊥ < a ≠ b ≡ c ≤ d ≪ ⊤ ⇒ (A ⇔ B),

  2H₂ d a+ O₂ ⇌  2H₂O